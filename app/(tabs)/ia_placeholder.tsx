import { View } from "react-native";

// Esta pantalla es un placeholder (marcador de posición)
// Sirve para anclar el botón FAB en la barra de pestañas.
// Nunca será visible para el usuario.
export default function IAPlaceholderScreen() {
  return <View style={{ flex: 1, backgroundColor: "white" }} />;
}
